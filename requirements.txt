# requirements.txt
# RTX 50シリーズ(sm_120)対応の依存関係リスト
# PyTorch Nightly Build必須（Blackwellアーキテクチャ sm_120サポート）

# PyTorch Nightly (RTX 50対応 - CUDA 12.8)
--index-url https://download.pytorch.org/whl/nightly/cu128
torch
torchvision
torchaudio

# torchdata (BonDNet dependency)
# Note: Version 0.7.1 is compatible with BonDNet
torchdata==0.7.1

# 化学構造処理
rdkit>=2023.9.1
mordred>=1.2.0
mol2vec>=0.1

# グラフニューラルネットワーク
# Note: torch-scatter等はDockerfileでCUDA対応ビルド済みホイールからインストール
torch-geometric>=2.5.0
ogb>=1.3.6

# DGL (Deep Graph Library) - BonDNetの依存関係
# Note: PyTorch Nightlyとの互換性のため、ソースからビルドが必要
# インストール手順（CUDA対応）:
#   export FORCE_CUDA=1 TORCH_CUDA_ARCH_LIST="9.0;12.0" CUDA_HOME=/usr/local/cuda-12.8
#   pip install git+https://github.com/dmlc/dgl.git
# Docker環境では自動的にソースからビルドされます

# BonDNet - BDE (Bond Dissociation Energy) 計算
# Note: DGLが先にインストールされている必要があります
# インストール手順:
#   pip install git+https://github.com/mjwen/bondnet.git
#   pip install pymatgen  # BonDNetの追加依存関係
# Docker環境では自動的にインストールされます
pymatgen>=2023.10.0

# データ処理
numpy>=1.24.0
pandas>=2.0.0
h5py>=3.10.0
scipy>=1.11.0

# 可視化
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.18.0
pillow>=10.0.0

# ユーティリティ
tqdm>=4.66.0
pyyaml>=6.0
tensorboard>=2.15.0
wandb>=0.16.0
torch-ema>=0.3.0

# 開発用
pytest>=7.4.0
black>=23.12.0
flake8>=7.0.0
mypy>=1.8.0
jupyter>=1.0.0
